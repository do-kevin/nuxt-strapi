{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest"
  },
  "devDependencies": {
    "@amiceli/vitest-cucumber": "^1.0.5",
    "@happy-dom/jest-environment": "^12.0.1",
    "@nuxt/devtools": "latest",
    "happy-dom": "^12.0.1",
    "nuxt": "^3.7.1",
    "nuxt-vitest": "0.10.3",
    "vitest": "0.33.0"
  },
  "dependencies": {
    "remark": "^14.0.3",
    "remark-html": "^15.0.2"
  }
}
